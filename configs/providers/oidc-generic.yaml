id: oidc-generic
display_name: Generic OIDC
grant_types:
  - authorization_code
  - refresh_token
auth_url: https://example.com/oauth2/v1/authorize
token_url: https://example.com/oauth2/v1/token
scopes:
  - openid
  - email
  - profile
  - offline_access
redirect_uri_templates:
  - "{api_base}/oauth/callback/{tenant}/{provider}"
token_endpoint_auth_methods:
  - client_secret_basic
  - client_secret_post
  - private_key_jwt
docs_url: https://openid.net/specs/openid-connect-core-1_0.html
notes: Generic OIDC provider descriptor used in tests.
metadata:
  service: generic-oidc
  category: identity
  compliance:
    gdpr: true
webhook_requirements:
  needs_webhook: false
