# Changelog

## Unreleased

- Host helpers now consume the broker’s resource-scoped token APIs via `OAuthBroker` (HTTP/NATS).
- Added propagation test coverage for TenantCtx and scopes through broker implementations.

